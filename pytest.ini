[pytest]
markers =
    pyinstaller_build: marks tests that build and test PyInstaller executables (slow tests)
    integration: marks integration tests (may require external dependencies like LibreOffice)
    packaged_app_test: marks tests for the final packaged application
    end_to_end: marks end-to-end tests that run on the packaged application
